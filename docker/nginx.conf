server {
    listen 80 default_server;
    server_name _;
    # Accept any host (IP or domain). When DNS points to this host, requests will be served here.
    # To restrict to a domain: server_name your-domain.com www.your-domain.com;
    # Nginx only needs public for try_files; PHP-FPM receives path below
    root /var/www/html/public;
    index index.php;

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        # Path must match app container: app lives at /var/www/html/app
        fastcgi_param SCRIPT_FILENAME /var/www/html/app/public/$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\. {
        deny all;
    }
}
